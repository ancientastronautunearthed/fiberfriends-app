// This page is intentionally left minimal to resolve a build error.
// The feature it belonged to was requested to be removed.
// Ideally, this file and its directory would be deleted by the build system.
export default function DoctorDashboardPlaceholderPage() {
  return null;
}
